# B1-1 학습 노트: 컴퓨터가 알아서 자기 상태를 점검하게 만들기

> **문과 중졸도 이해할 수 있게** — 일상어로 풀어 설명한 학습 노트

---

## 📖 목차
1. [초심자를 위한 용어집](#1-초심자를-위한-용어집)
2. [과제 해석 및 분석](#2-과제-해석-및-분석)
3. [과제를 진행하기 위한 기초](#3-과제를-진행하기-위한-기초)
4. [각 기초를 익히기 위한 간단한 체험 예제](#4-각-기초를-익히기-위한-간단한-체험-예제)
5. [과제를 작게 쪼개기: 잡 → 워크 → 워크플로우](#5-과제를-작게-쪼개기)
6. [워크플로우별 트레이드오프, 이슈, 트러블슈팅](#6-워크플로우별-트레이드오프-이슈-트러블슈팅)
7. [과제 완료 후 학습한 내용 정리](#7-과제-완료-후-학습한-내용-정리)

---

## 1. 초심자를 위한 용어집

### 🐧 Linux / 서버 관련

| 용어 | 쉬운 설명 | 비유 |
|------|-----------|------|
| **Linux** | 서버에서 가장 많이 쓰는 운영체제 | 건물의 관리 시스템 |
| **서버** | 인터넷에서 요청을 받아 응답하는 컴퓨터 | 식당의 주방 |
| **SSH** | 원격으로 서버에 접속하는 통로 | 건물의 출입문 |
| **포트 (Port)** | 데이터가 들어오는 문 번호 | 출입문 번호 (20022번) |
| **방화벽 (UFW)** | 들어오는 데이터를 걸러주는 문지기 | 건물 경비원 |
| **계정 (User)** | 서버에 접속하는 사용자 | 건물 출입증 |
| **그룹 (Group)** | 계정들을 묶은 단위 | 부서 (agent-common, agent-core) |
| **권한 (Permission)** | "이 파일을 읽을 수 있나? 쓸 수 있나?" | 출입증의 층별 권한 |
| **ACL** | 권한을 더 세밀하게 설정하는 도구 | 층별 + 방별 출입 권한 |
| **환경변수** | 프로그램이 실행될 때 참조하는 설정값 | 건물의 운영 매뉴얼 |

### 📜 셸 스크립트 관련

| 용어 | 쉬운 설명 | 비유 |
|------|-----------|------|
| **Bash** | 터미널에서 명령을 실행하는 언어 | 건물 관리자에게 지시하는 말 |
| **셸 스크립트** | 여러 명령을 하나의 파일로 묶은 것 | 관리 업무 매뉴얼 |
| **cron** | 정해진 시간에 자동으로 명령 실행 | 알람 시계 (매분 실행) |
| **monitor.sh** | 시스템 상태를 점검하는 스크립트 | 순찰 기록부 |
| **ps** | 실행 중인 프로세스 목록 | 건물 안에 있는 사람 목록 |
| **top** | 실시간 시스템 자원 사용 현황 | 건물 전력 사용 현황판 |
| **로그 (Log)** | "무슨 일이 언제 일어났는지" 기록 | 순찰 일지 |

### 🔒 보안 관련

| 용어 | 쉬운 설명 | 비유 |
|------|-----------|------|
| **Root** | 시스템의 최고 권한 계정 | 건물의 소유자 (모든 권한) |
| **sudo** | 일시적으로 관리자 권한 사용 | "이 것만 관리자 권한으로 할게요" |
| **PermitRootLogin** | Root의 원격 접속 허용 여부 | 소유자가 원격으로 출입 가능? |
| **레이트 리밋** | 너무 많이 요청하면 차단 | "1분에 1번만 문의 가능" |

---

## 2. 과제 해석 및 분석

### 2.1 한 줄 요약
**Linux 서버에 보안을 설정하고, 모니터링을 자동화하는 셸 스크립트를 만들어라.**

### 2.2 과제가 원하는 것
서버 운영의 기본을 "직접 해보는 것". 명령어를 외우는 게 아니라 "왜 이 명령어가 필요한지" 이해.

### 2.3 반드시 해야 하는 것
1. SSH 포트 20022로 변경 + Root 원격 접속 차단
2. 방화벽(UFW) 활성화, 20022/tcp + 15034/tcp만 허용
3. 계정 3개(agent-admin/dev/test) + 그룹 2개(agent-common/core) 생성
4. 디렉토리 권한 설정 (upload_files=공유, api_keys=보안)
5. Python 앱 실행 (Boot Sequence 5단계 [OK])
6. monitor.sh 구현 (프로세스/포트/CPU/MEM/DISK 점검 + 로그)
7. cron 매분 실행

### 2.4 제약 사항
- Bash만 사용 (Python 대체 금지)
- sudo 최소화 (일반 계정으로 진행)

---

## 3. 과제를 진행하기 위한 기초

### 3.1 기초 1: Linux 기본 명령어
- `ls`, `cd`, `chmod`, `chown`, `ps`, `ss`, `grep`, `awk`

### 3.2 기초 2: SSH 보안 설정
- `sshd_config` 파일 수정: Port, PermitRootLogin, PasswordAuthentication

### 3.3 기초 3: 방화벽 (UFW)
- `ufw default deny incoming`, `ufw allow 20022/tcp`

### 3.4 기초 4: 계정/그룹/권한
- `useradd`, `groupadd`, `usermod -aG`, `chmod`, `chown`, `getfacl`

### 3.5 기초 5: 셸 스크립트 작성
- 변수, 조건문(if), 반복문(for/while), 함수, `awk` 파싱

### 3.6 기초 6: cron
- `crontab -e`, `* * * * * /path/to/monitor.sh`

---

## 4. 각 기초를 익히기 위한 간단한 체험 예제

### 4.1 체험 1: SSH 포트 변경
```bash
sudo sed -i 's/#Port 22/Port 20022/' /etc/ssh/sshd_config
sudo systemctl restart sshd
ss -tulnp | grep sshd  # 20022번 확인
```

### 4.2 체험 2: 방화벽 설정
```bash
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 20022/tcp
sudo ufw --force enable
sudo ufw status verbose
```

### 4.3 체험 3: 계정/그룹 생성
```bash
sudo groupadd agent-common
sudo useradd -m -G agent-admin agent-admin
id agent-admin  # UID, GID, 그룹 확인
```

### 4.4 체험 4: monitor.sh 기본
```bash
#!/bin/bash
CPU=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}')
MEM=$(free | awk '/Mem/ {printf "%.1f", $3/$2*100}')
echo "[$(date '+%Y-%m-%d %H:%M:%S')] CPU:${CPU}% MEM:${MEM}%"
```

### 4.5 체험 5: cron 등록
```bash
crontab -e
# 매분 실행
* * * * * /home/agent-admin/agent-app/bin/monitor.sh
```

---

## 5. 과제를 작게 쪼개기: 잡 → 워크 → 워크플로우

### 5.1 전체 잡 분해도
```
Job 1: 기본 보안 설정 (SSH + 방화벽)
Job 2: 계정/그룹/권한 체계
Job 3: 애플리케이션 실행 환경
Job 4: monitor.sh 구현
Job 5: cron 자동 실행
```

### 5.2 실행 순서
```
Job 1 (보안) → Job 2 (계정) → Job 3 (앱 실행) → Job 4 (모니터링) → Job 5 (자동화)
```

---

## 6. 워크플로우별 트레이드오프, 이슈, 트러블슈팅

### 6.1 SSH 포트 22 → 20022
- **선택**: 포트 변경 → 자동 공격 99% 방어
- **트레이드오프**: 포기한 것 없음 (표준 포트 고집할 이유 없음)

### 6.2 UFW vs firewalld
- **선택**: UFW → 더 간단한 문법, Ubuntu 기본
- **트레이드오프**: firewalld의 영역별 정책 포기, 단순성 획득

### 6.3 권한 770 vs 750
- **선택**: upload_files=770(팀원 R/W), api_keys=770(core만)
- **트레이드오프**: 보안 vs 협업 편의 균형

### 6.4 트러블슈팅: cron이 안 돌아감
- **원인**: 권한 750인데 cron 실행 계정이 그룹에 없음
- **해결**: agent-admin을 agent-core 그룹에 추가

---

## 7. 과제 완료 후 학습한 내용 정리

### 7.1 배운 것
- "보안은 편의성과의 균형" — 모든 포트를 막으면 안 되고, 필요한 것만 열기
- "자동화는 사람을 대체하는 것이 아니라 사람을 돕는 것" — monitor.sh이 24시간 감시
- "로그는 장애 분석의 기반" — 로그 없으면 원인 추론 불가

### 7.2 핵심 인사이트
1. "최소 권한 원칙" — 필요한 것만 열고, 필요한 사람만 접근
2. "자동화는 필수" — 사람이 매분 확인할 수 없음
3. "로그가 곧 증거" — 장애 발생 시 로그가 유일한 단서
